# Report 01 - Baselines & infrastructure

LaTeX source for the first technical report (see `docs/PLAN.md` section 6).

Contents (to be added):
- `main.tex` - the report; use a clean preprint template.
- `refs.bib` - bibliography.
- `figures/` - generated **only** by `scripts/make_figures.py`, never by hand.

Scope: the Roborto infrastructure, reproduced SAC+HER (manipulation) and DD-PPO PointNav
(navigation) baselines, and the reproducibility findings. Every number is multi-seed
with confidence intervals; every figure is regenerable from a clean clone.
